"use client";
import {useState,FormEvent} from "react";
export function GoogleProfileForm({name,email}:{name:string;email:string}){
  const [type,setType]=useState("student"),[busy,setBusy]=useState(false),[error,setError]=useState("");
  async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch("/api/auth/google/complete",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();if(!j.success)throw Error(j.message);window.location.href=j.role==="admin"?"/admin":"/my-bookings";}catch(e){setError(e instanceof Error?e.message:"เชื่อมต่อไม่สำเร็จ");}finally{setBusy(false);}}
  return <form className="panel login-card" onSubmit={submit}><h1>ข้อมูลผู้จอง</h1><p>เชื่อมต่อ Google แล้ว: {email}</p><p>กรอกข้อมูลครั้งแรกเพื่อเริ่มใช้ระบบจองห้อง</p><div className="field"><label htmlFor="profile-name">ชื่อ–นามสกุล</label><input id="profile-name" name="name" defaultValue={name} required maxLength={120}/></div><div className="field"><label htmlFor="profile-type">ประเภทผู้จอง</label><select id="profile-type" name="requester_type" value={type} onChange={e=>setType(e.target.value)}><option value="student">นักศึกษา</option><option value="staff">บุคลากร</option></select></div>{type==="student"&&<div className="field"><label htmlFor="profile-student">รหัสนักศึกษา</label><input id="profile-student" name="student_id" required pattern="[0-9]{5,30}" inputMode="numeric"/></div>}<div className="field"><label htmlFor="profile-department">สาขาวิชา / หน่วยงาน</label><input id="profile-department" name="department" required maxLength={120}/></div>{error&&<p role="alert">{error}</p>}<button className="button button-primary login-button" disabled={busy}>{busy?"กำลังบันทึก…":"บันทึกและเริ่มใช้งาน"}</button><a href="/login">กลับหน้าเข้าสู่ระบบ</a></form>;
}
