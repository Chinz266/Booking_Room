"use client";
import {useState,FormEvent} from "react";
export function LoginForm({googleEnabled=false,googleMessage=""}:{googleEnabled?:boolean;googleMessage?:string}){const [register,setRegister]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState(googleMessage),[requesterType,setRequesterType]=useState("student");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch("/api/auth/"+(register?"register":"login"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();if(!j.success)throw new Error(j.message);if(register){setRegister(false);setMessage("สมัครสำเร็จ กรุณาเข้าสู่ระบบ");}else window.location.href=j.data.role==="admin"?"/admin":"/my-bookings";}catch(e){setMessage(e instanceof Error?e.message:"เชื่อมต่อไม่สำเร็จ");}finally{setBusy(false);}}return <form className="panel login-card" onSubmit={submit}><h1>{register?"สมัครสมาชิก":"เข้าสู่ระบบ"}</h1><p>บัญชีสำหรับจองห้องและติดตามรายการของคุณ</p>{googleEnabled&&<><a className="button button-secondary login-button" href="/api/auth/google">เข้าสู่ระบบด้วย Google</a><p className="login-divider">หรือใช้ชื่อผู้ใช้และรหัสผ่าน</p></>}{register&&<><div className="field"><label htmlFor="requester_type">ประเภทผู้สมัคร</label><select id="requester_type" name="requester_type" value={requesterType} onChange={e=>setRequesterType(e.target.value)}><option value="student">นักศึกษา</option><option value="staff">บุคลากร</option></select></div><div className="field"><label htmlFor="name">ชื่อ–นามสกุล</label><input id="name" name="name" required maxLength={120}/></div>{requesterType==="student"&&<div className="field"><label htmlFor="student_id">รหัสนักศึกษา</label><input id="student_id" name="student_id" required pattern="[0-9]{5,30}" inputMode="numeric"/></div>}</>}<div className="field"><label htmlFor="username">ชื่อผู้ใช้</label><input id="username" name="username" autoComplete="username" required maxLength={40}/></div><div className="field"><label htmlFor="password">รหัสผ่าน {register&&"(อย่างน้อย 10 ตัวอักษร)"}</label><input id="password" name="password" type="password" autoComplete={register?"new-password":"current-password"} required minLength={register?10:1} maxLength={128}/></div>{message&&<p role="status">{message}</p>}<button className="button button-primary login-button" disabled={busy}>{busy?"กำลังดำเนินการ…":register?"สมัครสมาชิก":"เข้าสู่ระบบ"}</button><button type="button" className="button button-secondary login-button" disabled={busy} onClick={()=>{setRegister(!register);setMessage("");}}>{register?"มีบัญชีแล้ว เข้าสู่ระบบ":"ยังไม่มีบัญชี สมัครสมาชิก"}</button></form>}
